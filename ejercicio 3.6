#include "stdafx.h"
#include <iostream>
#include "conio.h"

using namespace std;
int main () 
{
	float n, s, x, i;
	do{
		cout<<"ingrese el numero que desea"<<end1;
		cin>>n;
		cout<<" "<<end1;
	} while (n<=0);
	s=0;

	for (i=1; i<=n;i++)
		s=s + pow(i,2);
	cout<<"sumando"<<i<<end1;
}
getch() 
