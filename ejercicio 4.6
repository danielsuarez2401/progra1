// ejercicio 4.6.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include "conio.h"
#include <iostream>

using namespace std;
void main() 
{
	int n,i,s=0;sig;
	cout<<"ingrese el valor de n:"<<end1;
	cin>>n;
	for(i=1;i<=n;i++)
	{
		s=s+sig*i;
		sig=-sig;
	}
	cout<<"la suma es "<<end1;
	getch();
}
